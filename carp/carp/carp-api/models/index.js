const User = require('./User');
const Vehicle = require('./Vehicle');
const Trip = require('./Trip');
const Dashboard = require('./Dashboard');
const RoutePlanning = require('./RoutePlanning');

/**
 * @description Models index file - exports all models
 */
module.exports = {
  User,
  Vehicle,
  Trip,
  Dashboard,
  RoutePlanning
};
